# Installer test evidence

Generated by build/installer-test-evidence.py from the test run's TRX. **445 tests: 432 passed, 0 failed, 13 skipped.**

## The refusal matrix

| Test | Outcome |
|---|---|
| A blocking precheck is refused before anything is touched | Passed |
| A control payload altered on the medium is refused before anything is touched | Passed |
| A dry run of every mode touches nothing | Passed |
| A malformed service map is refused while the machine is clean | Passed |
| A payload that fails verification is refused before anything is touched | Passed |
| Another installer holding the lock is refused naming it | Passed |
| Install with no site pack is refused | Passed |
| Restore with no backup named is refused | Passed |

## By engine

| Engine (test class) | Tests | Passed | Skipped |
|---|---|---|---|
| BackupCryptoTests | 14 | 14 | 0 |
| BackupRestoreEngineTests | 13 | 13 | 0 |
| CliOptionsTests | 21 | 21 | 0 |
| CompositionRootTests | 5 | 5 | 0 |
| ConfigGeneratorTests | 18 | 18 | 0 |
| CurrentLinkTests | 11 | 11 | 0 |
| DatabaseBootstrapTests | 18 | 18 | 0 |
| HashVerifierTests | 7 | 7 | 0 |
| HealthAggregatorTests | 7 | 7 | 0 |
| InstallerLockTests | 7 | 7 | 0 |
| InstallerPipelineTests | 20 | 20 | 0 |
| InstallerStateMachineTests | 8 | 8 | 0 |
| LinuxPlatformEnginesTests | 17 | 17 | 0 |
| ManifestParserTests | 8 | 8 | 0 |
| MediaBuilderTests | 17 | 17 | 0 |
| PackEnginesTests | 12 | 12 | 0 |
| PackEnvelopeTests | 10 | 10 | 0 |
| PackLiveTests | 1 | 0 | 1 |
| PayloadConfigRewriterTests | 15 | 15 | 0 |
| PrecheckRunnerTests | 5 | 5 | 0 |
| PrecheckTests | 9 | 7 | 2 |
| RefusalMatrixTests | 8 | 8 | 0 |
| RepairEngineTests | 19 | 19 | 0 |
| SchemaDriftTests | 23 | 23 | 0 |
| SchemaFingerprintLiveTests | 10 | 0 | 10 |
| SecretGateTests | 27 | 27 | 0 |
| ServiceMapLoaderTests | 22 | 22 | 0 |
| SiteConfigLoaderTests | 17 | 17 | 0 |
| SystemdUnitWriterTests | 20 | 20 | 0 |
| TamperEvidenceTests | 20 | 20 | 0 |
| TopologyContractTests | 11 | 11 | 0 |
| UninstallAndMonitorTests | 8 | 8 | 0 |
| UpgradePathTests | 12 | 12 | 0 |
| VerifiedMediaTests | 5 | 5 | 0 |

## Skipped, and why

- `SchemaFingerprintLiveTests.Captures_tables_columns_indexes_keys_and_views` — EPACS_TEST_MYSQL is not set
- `SchemaFingerprintLiveTests.Composite_primary_key_order_is_preserved` — EPACS_TEST_MYSQL is not set
- `SchemaFingerprintLiveTests.Column_types_nullability_and_defaults_come_back_canonically` — EPACS_TEST_MYSQL is not set
- `SchemaFingerprintLiveTests.Identifiers_are_lower_cased_so_a_folding_node_produces_no_spurious_drift` — EPACS_TEST_MYSQL is not set
- `SchemaFingerprintLiveTests.Detects_a_real_mutation_end_to_end` — EPACS_TEST_MYSQL is not set
- `PackLiveTests.A_societys_rows_go_out_as_a_signed_pack_and_come_back_counted` — EPACS_TEST_MYSQL and EPACS_TEST_MYSQL_BIN (a directory with mysql and mysqldump) are not both set
- `SchemaFingerprintLiveTests.Foreign_keys_are_captured` — EPACS_TEST_MYSQL is not set
- `SchemaFingerprintLiveTests.The_fingerprint_hash_is_stable_across_captures_of_an_unchanged_database` — EPACS_TEST_MYSQL is not set
- `PrecheckTests.Os_release_fields_are_parsed_with_and_without_quotes` — Linux path
- `PrecheckTests.On_Linux_a_non_Debian_distribution_is_a_block_naming_the_ADR_never_a_Windows_version` — reads /etc/os-release semantics; the seam is exercised on Linux
- `SchemaFingerprintLiveTests.Acknowledged_drift_from_the_register_does_not_block` — EPACS_TEST_MYSQL is not set
- `SchemaFingerprintLiveTests.Indexes_carry_their_columns_in_SEQ_IN_INDEX_order_and_exclude_the_primary` — EPACS_TEST_MYSQL is not set
- `SchemaFingerprintLiveTests.A_capture_compared_with_itself_reports_no_drift` — EPACS_TEST_MYSQL is not set

## Every test

### BackupCryptoTests

- ✅ A file of any size round trips byte for byte(size: 0)
- ✅ A file of any size round trips byte for byte(size: 1)
- ✅ A file of any size round trips byte for byte(size: 4194303)
- ✅ A file of any size round trips byte for byte(size: 4194304)
- ✅ A file of any size round trips byte for byte(size: 4194305)
- ✅ A file of any size round trips byte for byte(size: 8400953)
- ✅ A file that is not ours is refused by its header
- ✅ A flipped byte is refused naming the chunk
- ✅ A truncated file is refused as truncated
- ✅ A wrong key is refused not garbage
- ✅ Encrypting the same file twice never reuses a nonce or produces the same bytes
- ✅ Key wrap round trips and a wrong kek is refused
- ✅ Manifest mac detects a single changed byte
- ✅ Recovery wrap round trips through an RSA key pair and names the key

### BackupRestoreEngineTests

- ✅ A configured recovery key that is missing refuses rather than silently dropping the wrap
- ✅ A flipped byte in the dump fails verification and restore changes nothing
- ✅ A missing mac is named
- ✅ A package is encrypted wrapped and maced and carries no plaintext
- ✅ A replacement node opens a package with the states recovery key
- ✅ A restore that leaves the database empty is a refusal naming the safety backup
- ✅ An altered manifest fails its mac
- ✅ Another node cannot open a package wrapped to this node only
- ✅ Manifest json round trips with the key wrap and never the package path
- ✅ Restore decrypts into root only staging feeds the dump to mysql counts and cleans up
- ✅ Restore with a safety backup takes one first and names it
- ✅ Verify reads the package back and confirms the dump is complete
- ✅ With a recovery key the data key is also wrapped to it

### CliOptionsTests

- ✅ A flag that needs a value and has none is an error
- ✅ A fully specified purge parses
- ✅ An unknown mode is an error that lists the valid ones
- ✅ An unrecognised argument is an error and never ignored
- ✅ Apply arms the run in either syntax(arg: "--apply")
- ✅ Apply arms the run in either syntax(arg: "/apply")
- ✅ Dry run is the default
- ✅ Help short circuits validation
- ✅ Omitting mode means auto detect
- ✅ Parses mode case insensitively(text: "UNINSTALL", expected: Uninstall)
- ✅ Parses mode case insensitively(text: "Upgrade", expected: Upgrade)
- ✅ Parses mode case insensitively(text: "install", expected: Install)
- ✅ Purge data is allowed in a dry run without a token
- ✅ Purge data outside uninstall is refused
- ✅ Purge data with apply demands a token and a typed confirmation
- ✅ Quiet and verbose contradict each other
- ✅ Usage documents every exit code the program can return
- ✅ Usage leads with the dry run default
- ✅ epcfg")
- ✅ epcfg")
- ✅ epcfg\"")

### CompositionRootTests

- ✅ Every precheck that exists is registered
- ✅ The container builds with every dependency resolvable
- ✅ The override token validator denies by default
- ✅ The pipeline resolves
- ✅ The site config loader and state machine factory resolve

### ConfigGeneratorTests

- ✅ A missing template directory is fatal rather than skipped
- ✅ A service in the topology but not in options is still addressable
- ✅ A template that would produce invalid json is not written
- ✅ Addresses any application service by name
- ✅ An empty template directory is fatal
- ✅ An unresolved token aborts generation
- ✅ Every unresolved token is listed at once
- ✅ No output file is left behind when a token is unresolved
- ✅ Non json templates are not escaped
- ✅ Output is written atomically leaving no temp file
- ✅ Scales to the full estate service count
- ✅ Service names resolve case insensitively
- ✅ Site identity reaches the generated file
- ✅ The shipped site template resolves completely
- ✅ The shipped template carries no password
- ✅ The shipped template overrides the hardcoded linux log path
- ✅ The shipped template sets both connection string keys
- ✅ Windows paths are escaped so the generated json parses

### CurrentLinkTests

- ✅ A leftover staging link from an earlier cut does not block the next switch
- ✅ A marker left behind after a completed switch is just cleared
- ✅ A marker whose target has vanished leaves current alone
- ✅ An interrupted switch is completed on the next run
- ✅ Points current at a release
- ✅ Re staging after an interrupted attempt replaces the partial copy
- ✅ Recovery is a no op when no switch was in flight
- ✅ Refuses to point current at a release that is not there
- ✅ Staging places a release without switching to it
- ✅ Switching between releases replaces the link without touching either release
- ✅ The intent marker is cleared on success

### DatabaseBootstrapTests

- ✅ Accepting case folding is off by default
- ✅ Case folding can be accepted deliberately
- ✅ Config binds to localhost only
- ✅ Config carries a connection ceiling the estate can survive
- ✅ Config expands the data root token
- ✅ Config is durable because a PACS node loses power
- ✅ Config records why the case setting is what it is
- ✅ Config sets lower case table names from the verdict
- ✅ Config uses the estate character set and collation
- ✅ Config warns against editing it on the host
- ✅ Passwords never reach a command line
- ✅ Refuses to bootstrap on a case insensitive volume
- ✅ Refuses to re initialise a populated data directory
- ✅ Refuses when the baseline ddl is absent
- ✅ The estate setting is zero and is stated as a constant
- ✅ The guard leaves no probe files behind
- ✅ The guard probes the actual path rather than assuming from the OS
- ✅ The process runner redacts secrets from captured output

### HashVerifierTests

- ✅ ComputeHashAsync EmptyFile ReturnsEmptyFileHash
- ✅ ComputeHashAsync KnownContent ReturnsExpectedHash
- ✅ ComputeHashAsync NonExistentFile ThrowsFileNotFoundException
- ✅ ComputeHashAsync SameContentProducesSameHash
- ✅ VerifyAsync CaseInsensitiveHash ReturnsTrue
- ✅ VerifyAsync CorrectHash ReturnsTrue
- ✅ VerifyAsync IncorrectHash ReturnsFalse

### HealthAggregatorTests

- ✅ A closed port is Failed after the window and the report names it
- ✅ A command check uses the exit code the map names
- ✅ A service that comes up inside the window passes
- ✅ A tcp check that connects is Listening not Healthy
- ✅ An http check against the agents own endpoint is Healthy for live and Failed for ready until flipped
- ✅ An unresolved token in the check is the maps fault and is said so
- ✅ Checks run in parallel so 27 services cost one window not 27

### InstallerLockTests

- ✅ Acquires on a clean machine
- ✅ Dispose releases
- ✅ Reacquiring while already held by this instance is a no op
- ✅ Refuses a second holder while the first holds it
- ✅ Release from another thread does not throw
- ✅ Release is idempotent
- ✅ Releases so the next run can proceed

### InstallerPipelineTests

- ✅ A backup that does not verify is reported as no way back
- ✅ A control payload altered on the stick stops the install before anything is touched
- ✅ A mode with no engine reports NotImplemented and never success(mode: Backup)
- ✅ A service that does not come up fails the install by name
- ✅ A warning precheck does not block
- ✅ An engine this platform does not have exits NotImplemented by name never success
- ✅ Apply runs every step in order and checkpoints
- ✅ Backup dry run takes nothing
- ✅ Backup takes a package and then verifies it by reading it back
- ✅ Dry run fails on a malformed topology before it would have installed
- ✅ Dry run verifies and prechecks but touches nothing
- ✅ Health is judged in three verdicts and listening is never called healthy
- ✅ Install binds the site tokens and keeps the site pack on the node
- ✅ Install loads the site data pack after the baseline and says so when there is none
- ✅ Refuses an install with no site config
- ✅ Refuses to run while another installer holds the lock
- ✅ Stops and changes nothing when a precheck blocks
- ✅ Stops and changes nothing when the payload does not verify
- ✅ The least privilege boundary is laid before a service is registered
- ✅ Uninstall refuses when the installed service map is missing

### InstallerStateMachineTests

- ✅ A completed or failed previous run needs no recovery
- ✅ A run that died mid phase is offered for recovery and the checkpoint moves to Recovery
- ✅ A torn checkpoint is a clean start not a crash
- ✅ Complete and Fail are terminal and Fail records the error
- ✅ Every transition is on disk before it returns
- ✅ No state file is a clean start
- ✅ Starts in Load with the target version and a fresh correlation id
- ✅ The checkpoint survives a json round trip with every field

### LinuxPlatformEnginesTests

- ✅ Accounts are distinct in map order without root or LocalSystem
- ✅ Apply checks the ruleset before loading it and refuses on a check failure
- ✅ Apply runs exactly the stated commands and creates a missing directory
- ✅ Apply stops on the first failed command and names it
- ✅ Ensure creates only the missing accounts as system accounts
- ✅ Ensure fails loudly when useradd fails
- ✅ Firewall rules keep the database off the LAN and open the application ports
- ✅ None is root only 0700 for a leaf and 0755 for a traversable root
- ✅ Not yet built engines refuse at use naming the task and never at construction
- ✅ ReadOnly is root owned with the reader as group
- ✅ ReadWrite is chown to the account and 0750
- ✅ Remove deletes only our table
- ✅ Rules for the generated topology cover every declared data directory
- ✅ Rules give each writer its own directories and the services read only binaries and config
- ✅ Ruleset is one owned table with drop by default loopback open and the stated ports
- ✅ Verify reads owner and mode and names every mismatch
- ✅ Verify reports ports absent from the live table

### ManifestParserTests

- ✅ ParseFile NonExistentFile ThrowsFileNotFoundException
- ✅ Parse EmptyString ThrowsArgumentException
- ✅ Parse MissingManifestSection ThrowsInvalidOperationException
- ✅ Parse NoPayloads ThrowsInvalidOperationException
- ✅ Parse ValidYaml ReturnsCorrectCompatibility
- ✅ Parse ValidYaml ReturnsCorrectPayloadCount
- ✅ Parse ValidYaml ReturnsManifestWithCorrectMetadata
- ✅ Parse ValidYaml ReturnsPayloadsWithCorrectProperties

### MediaBuilderTests

- ✅ A built medium verifies with the installers own verifier
- ✅ A medium signed by the wrong key is refused
- ✅ A medium with no payloads is refused
- ✅ A missing payload file is caught
- ✅ A payload whose source does not exist stops the build
- ✅ A tampered manifest is caught by the signature
- ✅ A tampered payload is caught
- ✅ An unsigned medium is built but reported as unsigned
- ✅ Enabling the group puts the payload back
- ✅ Hashes are measured not declared
- ✅ Payloads outside the requested groups are left off the medium
- ✅ Signing without a certificate refuses rather than producing an unsigned manifest
- ✅ The build is byte reproducible
- ✅ The output directory is cleaned so stale payloads cannot survive
- ✅ The shipped sample spec builds and verifies
- ✅ The shipped sample spec does not reference the stale schema dump
- ✅ The signature covers the manifest and therefore every payload hash

### PackEnginesTests

- ✅ A configured site key that is missing refuses rather than going out unsigned
- ✅ A count that falls short after apply is not recorded as applied
- ✅ A ledger pack carries only this societys rows from society tables counted and signed
- ✅ A pack cut against another schema is rejected naming upgrade
- ✅ A pack signed by someone other than the pinned state key is rejected
- ✅ A policy pack is applied counted and recorded
- ✅ A site data pack cut for another baseline is refused and a node with no record cannot match one
- ✅ Consecutive packs chain and a lost pack is re cut identically in content
- ✅ Replays are acknowledged gaps wait and tampering is rejected with the reason beside the pack
- ✅ Site data is the zeroth policy pack and lands a society from nothing
- ✅ With the states public key the pack is encrypted to it
- ✅ Without a site key the pack is written unsigned and says so

### PackEnvelopeTests

- ✅ A data file altered after signing is refused by hash
- ✅ A manifest altered after signing is refused
- ✅ A missing data file is refused by hash too
- ✅ A signed pack round trips with measured hashes and counts
- ✅ An encrypted pack carries no plaintext and opens only with the recipients key
- ✅ An unsigned pack is refused where signatures are required and accepted on a bench
- ✅ Scope sequence chain replay and schema each refuse by name
- ✅ The ledger persists cursors atomically and starts at genesis
- ✅ The next pack chains to the hash of the previous manifest
- ✅ The wrong signer is refused before a byte of content is read

### PackLiveTests

- ⏭ A societys rows go out as a signed pack and come back counted

### PayloadConfigRewriterTests

- ✅ A sibling file without a services object is refused not ignored
- ✅ Merge is deep for objects whole for arrays and ignores commentary
- ✅ Refuses a file that does not parse rather than rewriting it
- ✅ Refuses a service that runs from services but has no configuration
- ✅ Rewriting twice is idempotent
- ✅ Sibling urls use the section the module actually reads
- ✅ WithPassword quotes a value the connector would misparse
- ✅ WithPassword replaces any existing credential(input: "Server=a;Password=old;Uid=u", expected: "Server=a;Uid=u;Pwd=new")
- ✅ WithPassword replaces any existing credential(input: "Server=a;Uid=u", expected: "Server=a;Uid=u;Pwd=new")
- ✅ WithPassword replaces any existing credential(input: "Server=a;Uid=u;Pwd=old", expected: "Server=a;Uid=u;Pwd=new")
- ✅ Without a password it says so and writes none
- ✅ Writes overlay sibling urls and credential into the service file
- ✅ json", section: "APIKeys")
- ✅ json", section: "APIKeys")
- ✅ json", section: "ERPKeys")

### PrecheckRunnerTests

- ✅ RunAllAsync AllPass CanProceedIsTrue
- ✅ RunAllAsync CheckThrowsException RecordedAsBlocking
- ✅ RunAllAsync ExecutesInOrder
- ✅ RunAllAsync OneBlocking CanProceedIsFalse
- ✅ RunAllAsync WarningOnly CanProceedIsTrue

### PrecheckTests

- ✅ Admin check reports the truth about this process
- ✅ Disk check blocks when the data volume cannot hold the minimum
- ⏭ On Linux a non Debian distribution is a block naming the ADR never a Windows version
- ✅ Os check passes on this machine or names ADR 0010
- ⏭ Os release fields are parsed with and without quotes
- ✅ Pending reboot check is not a windows only crash elsewhere
- ✅ Port check names a port something else holds and passes when all are free
- ✅ Ram check blocks below the minimum warns below the recommendation and passes otherwise
- ✅ The runner orders by Order and a single block stops the run

### RefusalMatrixTests

- ✅ A blocking precheck is refused before anything is touched
- ✅ A control payload altered on the medium is refused before anything is touched
- ✅ A dry run of every mode touches nothing
- ✅ A malformed service map is refused while the machine is clean
- ✅ A payload that fails verification is refused before anything is touched
- ✅ Another installer holding the lock is refused naming it
- ✅ Install with no site pack is refused
- ✅ Restore with no backup named is refused

### RepairEngineTests

- ✅ A config on the command line wins over the installed copy
- ✅ A dry run diagnoses everything and changes nothing
- ✅ Always re registers services
- ✅ Apply re lays accounts ownership and firewall before re registering
- ✅ Dry run names permission drift without touching it
- ✅ Leaves intact binaries alone unless asked
- ✅ Notices a missing release directory
- ✅ Notices missing configuration
- ✅ Notices that current points at nothing
- ✅ On a platform without the engines the dry run still answers and names the gap
- ✅ Re lays binaries when the release is missing
- ✅ Re lays intact binaries when asked
- ✅ Reads the installed site pack when no config is given and binds it
- ✅ Refuses a medium carrying a different version
- ✅ Refuses before stopping anything when the map needs a site and none is known
- ✅ Refuses media that does not verify
- ✅ Refuses to regenerate configuration without a site pack
- ✅ Regenerating configuration is reported as requested not broken
- ✅ Says plainly that data was not touched

### SchemaDriftTests

- ✅ A changed column type is breaking even when it looks like a widening
- ✅ A changed default is only compatible
- ✅ A changed nullability is breaking in both directions
- ✅ A missing column is breaking
- ✅ A missing index is compatible not benign
- ✅ A missing table is breaking and uses the estates key
- ✅ A nullability change does not also report a default change
- ✅ A register entry with no stated reason is ignored
- ✅ A reordered primary key is breaking
- ✅ A stale acknowledgement is reported
- ✅ Acknowledging one finding does not hide another
- ✅ An acknowledged finding is reported but does not count
- ✅ An extra NOT NULL column WITH a default is benign
- ✅ An extra NOT NULL column with no default is BREAKING
- ✅ An extra nullable column is benign
- ✅ An extra table is benign
- ✅ An identical schema reports nothing
- ✅ Foreign keys are compared both ways
- ✅ No default and a default of NULL are different columns
- ✅ Same index name over different columns is its own finding
- ✅ The estates real register parses
- ✅ The register parses the estates own format
- ✅ The rendered report leads with the verdict and names stale entries

### SchemaFingerprintLiveTests

- ⏭ A capture compared with itself reports no drift
- ⏭ Acknowledged drift from the register does not block
- ⏭ Captures tables columns indexes keys and views
- ⏭ Column types nullability and defaults come back canonically
- ⏭ Composite primary key order is preserved
- ⏭ Detects a real mutation end to end
- ⏭ Foreign keys are captured
- ⏭ Identifiers are lower cased so a folding node produces no spurious drift
- ⏭ Indexes carry their columns in SEQ IN INDEX order and exclude the primary
- ⏭ The fingerprint hash is stable across captures of an unchanged database

### SecretGateTests

- ✅ 131:5010/api/v1/")
- ✅ 4;Uid=root;Password=devpass", expected: True)
- ✅ 4;Uid=root;Pwd=devpass", expected: True)
- ✅ A clean payload has no findings
- ✅ A file that does not parse cannot be cleared
- ✅ A real module on r2 dev stable still carries TD 123 and the gate says so
- ✅ A url that smuggles a credential in its query string is flagged
- ✅ An allow pattern over the path clears a fixture value
- ✅ Does not flag what the estate does not flag(key: "ClientSecret", value: "${epcfg:client secret}")
- ✅ Does not flag what the estate does not flag(key: "ClientSecret", value: "REPLACE VIA SECRETS")
- ✅ Does not flag what the estate does not flag(key: "Description", value: "a perfectly ordinary sentence that is long")
- ✅ Does not flag what the estate does not flag(key: "Password", value: "short")
- ✅ Does not flag what the estate does not flag(key: "Password", value: "xxxxxxxxxx")
- ✅ Does not flag what the estate does not flag(key: "TokenUrl", value: "api/Token")
- ✅ Flags what the estate flags(key: "AdminPassword", value: "Sup3rSecret!", expected: True)
- ✅ Flags what the estate flags(key: "ClientSecret", value: "b6c5a0f2-8c2f-4e1a-9b7e-0d1c2e3f4a5b", expected: True)
- ✅ Flags what the estate flags(key: "SenderPassword", value: "gmail-app-password-1234", expected: True)
- ✅ Flags what the estate flags(key: "XApiKey", value: "ac7d6e3b7f1a4c2e9d8b", expected: True)
- ✅ Flags what the estate flags(key: "aadharapikey", value: "MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA7", expected: True)
- ✅ Flags what the estate flags(key: "aadharkey", value: "shortish-but-real", expected: True)
- ✅ Mask never reveals more than the ends
- ✅ Scans every appsettings file in a directory and names file and key
- ✅ Scans inside a zip payload too
- ✅ The builder honours an allow pattern
- ✅ The builder refuses a payload with a credential before staging a byte
- ✅ The rules are byte equal to config hygiene py
- ✅ in/APCOBAPI/ThirdPar"···)

### ServiceMapLoaderTests

- ✅ Applies defaults for omitted optional fields
- ✅ Default components install core and cache but not eventing
- ✅ Disabling the cache leaves only core
- ✅ Enabling eventing brings kafka in
- ✅ Filters the harness map by group
- ✅ Loads the canonical infrastructure service map
- ✅ Loads the harness service map including unmodelled keys
- ✅ Missing file is fatal rather than an empty topology
- ✅ Preserves every health check type in the canonical map
- ✅ Rejects a duplicate service name
- ✅ Rejects a half specified health check(healthCheckType: "type: \"command\"", expected: "requires 'command'")
- ✅ Rejects a half specified health check(healthCheckType: "type: \"http\"", expected: "requires 'url'")
- ✅ Rejects a half specified health check(healthCheckType: "type: \"smoke-signal\"", expected: "unknown health check type")
- ✅ Rejects a half specified health check(healthCheckType: "type: \"tcp\"", expected: "requires 'port'")
- ✅ Rejects a service with no executable
- ✅ Rejects a service with no health check
- ✅ Rejects an empty map
- ✅ Rejects malformed yaml with a line number
- ✅ Returns services in start order
- ✅ The cache provider defaults to what the estate actually runs
- ✅ The canonical map declares a group on every service
- ✅ Ungrouped services survive group filtering

### SiteConfigLoaderTests

- ✅ A pack altered after signing is refused
- ✅ A pack signed by an impostor is refused
- ✅ Accepts an unsigned pack only when explicitly allowed
- ✅ An embedded signature field alone is refused because no build ever verified one
- ✅ Loads a pack whose detached signature verifies against the pinned release key
- ✅ Reads the sample pack that ships in this repository
- ✅ Refuses a malformed state code(stateCode: "A1")
- ✅ Refuses a malformed state code(stateCode: "ANDHRA")
- ✅ Refuses a malformed state code(stateCode: "a")
- ✅ Refuses a malformed state code(stateCode: "ap")
- ✅ Refuses a missing file
- ✅ Refuses a pack missing a required field(field: "data root")
- ✅ Refuses a pack missing a required field(field: "pacs id")
- ✅ Refuses a pack missing a required field(field: "state code")
- ✅ Refuses an unsigned pack by default
- ✅ Refuses malformed json rather than defaulting
- ✅ Refuses the sample signature placeholder

### SystemdUnitWriterTests

- ✅ A service that must not restart maps to Restart no
- ✅ An unknown token aborts rather than registering a literal
- ✅ Carries the same load bearing properties as the estate template
- ✅ Data directories become ReadWritePaths
- ✅ Environment lines are ordered so the unit is reproducible
- ✅ Environment values are token resolved
- ✅ Environment variables reach the unit
- ✅ Hardening is modest on purpose
- ✅ No ReadWritePaths line when the service declares no directories
- ✅ No environment block when the service declares none
- ✅ Renders every service in the shipped map without throwing
- ✅ Reset window comes from the service map
- ✅ RestartSec is never zero
- ✅ Runs as the declared unprivileged account
- ✅ Service ports from the infrastructure vocabulary are resolved
- ✅ Sets the file descriptor limit the connection pools require
- ✅ Units are prefixed so they can never collide with the estate or the distro
- ✅ Warns against editing it on the host
- ✅ Windows separators in the service map are translated
- ✅ restart and bundle still maps to on failure

### TamperEvidenceTests

- ✅ 1:5010/api/v1/\"")
- ✅ 1;Uid=epacs app;Pwd=p4s"···, secret: "p4ss-w0rd-9")
- ✅ 1;Uid=epacs app\"")
- ✅ 1\"")
- ✅ A bundle carries redacted config and state and is encrypted to the state when a key is configured
- ✅ A deleted entry breaks the chain at the gap
- ✅ A new process continues the chain from the file
- ✅ An empty chain verifies as empty
- ✅ Entries chain and a fresh reader verifies the whole file
- ✅ Generated passwords are long random and never repeat
- ✅ Rotation produces a different password and keys are created once
- ✅ Secrets round trip and never sit in the file in clear
- ✅ Tampering with entry N is detected at N
- ✅ The bundle redacts what it must never carry(content: "Aadhaar 1234 5678 9012 on file", secret: "1234 5678")
- ✅ The bundle redacts what it must never carry(content: "\"ClientSecret\": \"b6c5a0f2-8c2f-4e1a\"", secret: "b6c5a0f2")
- ✅ The bundle redacts what it must never carry(content: "\"DefaultConnection\": \"Server=x;Password=devpass"···, secret: "devpass")
- ✅ The bundle redacts what it must never carry(content: "\"SenderPassword\": \"gmail-app-password-1234\"", secret: "gmail-app-password-1234")
- ✅ The bundle redacts what it must never carry(content: "\"aadharapikey\": \"MIIBIjANBgkqhkiG9w0BAQEF\"", secret: "MIIBIjAN")
- ✅ The master key is random and on disk root only and a copy of the store without it is useless
- ✅ Without a recovery key the bundle is a plain zip and the log says so

### TopologyContractTests

- ✅ A generated entry cannot be resolved without a site and the refusal names the token
- ✅ A generated entry renders to a unit with the site state once the site is bound
- ✅ Application services start after the infrastructure and before the agents in the estates tiers
- ✅ Applications json and the map agree on every port
- ✅ Applications json binds into ServicesOptions with 27 entries
- ✅ Every application service selects its state from the site pack
- ✅ Health checks are recorded never invented
- ✅ Linux map carries the infrastructure and all 27 application services
- ✅ Ports are unique across the whole node
- ✅ SiteTokenSource is empty until bound and carries the site after
- ✅ Windows map loads with the same service set

### UninstallAndMonitorTests

- ✅ Config drift survives a restart and names the changed and missing files
- ✅ Config drift with no baseline says so rather than passing
- ✅ Disk space monitor reads the real volume and does not throw
- ✅ Purge with a token the validator refuses is refused and the data survives
- ✅ Purge with a valid token and the exact confirmation removes the data
- ✅ Purge with a valid token but the wrong typed confirmation is refused
- ✅ Purge without a token is refused and the data survives
- ✅ Uninstall stops deregisters removes the firewall table and binaries and keeps the data

### UpgradePathTests

- ✅ 0")
- ✅ 0")
- ✅ 0", to: "not-a-version")
- ✅ 99")
- ✅ A downgrade is refused and says what to do instead
- ✅ A downgrade is refused even when the window would allow it
- ✅ A forward step is allowed
- ✅ Flags from the manifest are carried through
- ✅ Inside the declared window is allowed
- ✅ Reinstalling the same version is refused
- ✅ The window boundaries are inclusive
- ✅ With no declared window a forward step is allowed

### VerifiedMediaTests

- ✅ A control payload altered after the manifest was built is refused naming it
- ✅ A listed control payload that is not on the stick is refused
- ✅ A loose file beside the manifest is never read
- ✅ A missing control payload is a refusal at the point of use not a fallback to the stick
- ✅ Control payloads are extracted and resolve by name

